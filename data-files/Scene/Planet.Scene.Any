// -*- c++ -*-
{
    name = "Planet";
    
    models = {
       cubeModel = ArticulatedModel::Specification {
            filename = "wtf.obj";
            preprocess = {
                setMaterial(all(), Color3(1, 0, 0));
                transformGeometry(all(), Matrix4::scale(1, 1, 1));
            };
       };
    };
    
    entities = {
       skybox = Skybox {
            texture = "cubemap/whiteroom/whiteroom-*.png";
       };

        cube0 = VisibleEntity {
            model = "cubeModel";
            frame = CFrame::fromXYZYPRDegrees(0, 0, -2, 45, 0, 0);
        };

        camera = Camera {
            frame = CFrame::fromXYZYPRDegrees(8.4593, 1.9692, -0.10269, 76.203, -9.7402, 0);
        };
    };
};